# PLAN — этапы и текущий фокус

Единственный источник «что делаем сейчас» на уровне этапов. Задачи с критериями приёмки — в [BACKLOG.md](BACKLOG.md). Изменился план → сначала обнови этот файл и бэклог, потом код.

## Текущий фокус

BS-1 сделан (бот @Bytoviy_Serbian_bot), ждём проверки владельца. Следующая ready-задача — BS-2 (тренировка через Claude); для неё нужен ANTHROPIC_API_KEY в `.env`.

## Этапы

| Этап | Задачи | Статус |
|------|--------|--------|
| 0. Инфраструктура агентной разработки | BS-0 | ✅ готово (2026-07-04) |
| 1. Скелет бота: /start, меню, 3 тестовых сценария, «Мне срочно», без AI | BS-1 | ✅ готово (2026-07-04) |
| 2. Ролевая тренировка через Claude API | BS-2 | ⬜ |
| 3. Лог событий в SQLite | BS-3 | ⬜ |
| 4. README, деплой (Railway) | BS-4 | ⬜ |
| 5. Полный контент: 13 сценариев | BS-5 | ⬜ |

## Целевая структура кода

```
bot/
├── main.py            # точка входа: Dispatcher, роутеры, long polling
├── config.py          # чтение .env
├── content.py         # загрузка data/scenarios.json при старте
├── keyboards.py       # инлайн-клавиатуры, callback-данные
├── ai.py              # обёртка над Claude API
├── db.py              # SQLite: init + log_event()
└── handlers/
    ├── start.py       # /start, меню, «Мне срочно»
    ├── scenarios.py   # карточка сценария
    └── training.py    # ролевой диалог (FSM)
data/scenarios.json    # весь контент
```
